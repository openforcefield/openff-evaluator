# .readthedocs.yml

version: 2

build:
  image: latest

python:
  version: 3.6 
  install:
    - method: setuptools
      path: propertyestimator

# Build PDF in addition to default HTML and JSON
formats:
    - pdf

conda:
  environment: docs/environment.yml
