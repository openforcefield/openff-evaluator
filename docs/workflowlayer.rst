The ``WorkflowLayer``
========================

The ``WorkflowLayer`` is serves as a base ``CalculationLayer`` for any layers which will employ the built-in
``Workflow`` engine for performing calculations.
