name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov
    - --extra-index-url https://pypi.anaconda.org/OpenEye/simple
    - OpenEye-toolkits
